/**
 * data/tripData.ts
 * ─────────────────────────────────────────────────────────────────────────────
 * Backward-compat shim — all existing imports (TimelineScrubber,
 * DetailPanel, App) continue to work unchanged.
 *
 * New code should import directly from 'data/index' or specific trip files.
 */
export * from './index';